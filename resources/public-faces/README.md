# 公众人物人脸

这里是项目资源，不是正式照片库。

- `references/` 按人放参考图，从来源文件夹复制，不删除原件。
- `public-faces.sqlite3` 单独保存姓名、别名和人脸特征。里面没有私人库编号，也没有绝对路径。
- 来源里文件名以 `incoming-`、`najia-`、`marco-polo`、`040b` 开头的照片不会进来。名人卡片上挂着的其他私人照片也不会进来。

重新生成用 `tools/seed_public_faces.py`。它只读正式库和来源文件夹。
