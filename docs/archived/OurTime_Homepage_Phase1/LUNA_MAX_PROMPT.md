请在当前本地 OurTime-PictureViewer（拾光）项目中，执行“首页 Phase 1”接入与改版，不要只提交方案。

项目根目录有 `OurTime_Homepage_Phase1_2026-09-11.zip`；解压后的目录名为 `OurTime_Homepage_Phase1`。这是首页包，不是此前的大图查看器补丁。若尚未解压，只解压到这个独立目录，不覆盖 `web/`。若已有同名目录，先比较，保留已有修改。

先读本地 README、AGENTS、近期任务卡，再读包内 `00_README.md`、`BASELINE.md`、`integration/IMPLEMENTATION.md`、`integration/ADAPTER-CONTRACT.md` 和 `ACCEPTANCE-CHECKLIST.md`。本包没有取得完整远程 checkout，不能视为与你本地版本完全匹配；本地当前代码及已有未提交改动优先。

本轮目标：
1. 首页工具栏收敛为“搜索、筛选、选择、排序”。首页标题显示“全部照片”，默认最新优先；保留内部 `timeline` 等状态标识，不做全局重命名。
2. 筛选抽屉默认关闭；只有点击才出现。本阶段只接真实已有人物和文件夹。修改条件是草稿，应用时一次生效并关闭；取消不生效。无条件时标签行不占空间。
3. 筛选不能自动改变排序。条件标签逐项移除；“清除全部”不改变排序。
4. “选择”进入既有批量模式，复用当前屏幕选择、清空、补录、排除及原有确认流程。不新增删除原文件，不改变选择上限或可见范围定义。
5. 首页去掉重复时间排序、目录常驻控件。年份入口只复用现有 years 页面；不新增第二个时间线，不破坏原功能的唯一入口。

实施要求：
- 先记录工作区状态和受影响文件的本轮前副本/哈希，运行包内 `tools/preflight.py --repo .`。
- `01-add-files.patch` 只新增三个模块文件。检查无冲突后可应用，但它不会自动接入首页；不能应用完就报告完成。
- 直接复用包内 `home-query-ui.js`、`home-query-ui.css`、`home-query-bridge.js`。在本地做一个小的 init/接线，绑定真实 state、现有重建照片列表函数、人物数据源、文件夹选择器、选择模式与批量处理函数。不要重新写一套筛选组件或照片加载器。
- 逐项核对 adapter 合同，特别是“重建列表”不能误接成“加载下一页”、人物选项不能只取一页、文件夹选择不能提前应用或扫描、人物页跳回首页和勾选照片后必须同步新 UI。不要用隐藏按钮的 `.click()` 代替正确接线。
- 保留 app.js → viewer.js → waterfall.js 的相对顺序。新增组件挂载成功后才停用首页旧入口，避免两套监听同时刷新；被其他代码引用的旧 DOM 不能直接删坏。
- 只允许首页相关的小范围 HTML/JS 接线和文案调整；新增样式仅作用于该组件。保留原照片网格、缩略图说明高度、查看器和其他页面布局。

禁止改 Python、数据库/schema/data、扫描/人脸/排除规则、依赖、`viewer.js`、`waterfall.js` 或 viewer 的 CSS/DOM。不要重启/停止现役服务，不触发扫描或识别。不要加入日期范围、结构化地点筛选、智能查询解析、假标签、占位按钮或导航大重构。不要 git reset --hard、clean、强制覆盖、自动 commit/push。

验证：先做 Node 语法检查和本地既有 `validation/smoke.py`；再运行包内模块自测。模块自测是内存夹具，不需要应用服务。按验收清单做实际页面只读检查，覆盖查询组合、排序、选择、人物跳转、年份入口、大图翻页/返回位置和窄屏。正式库不得提交排除、恢复、补录或合并；写操作只用隔离数据验证。缺少验证环境时标记未验证，不把模块测试算成真实项目通过。

遇到可局部解决的差异，按本地实际代码合理合并并记录；只有确实需要越过上述禁止边界或存在重大冲突时才暂停说明，不要擅自扩大范围。

最终给我：实际变更文件与接线位置、运行过的测试及通过/失败/跳过、未验证或未完成事项、回退方法。不要再画设计效果图，也不要用“理论上能用”代替验证结果。
