# 拾光第二轮返工任务卡

任务号：PHOTO-REPAIR-20260910-R2。目录：G:\Trea\照片浏览器。

目标：修复剩余缺陷，补齐验收，使代码、行为、证据和报告一致。

阅读：AGENTS.md、README.md → 本卡 → validation/reports/independent-review-20260910/REVIEW.md → docs/修复任务卡-20260910.md → 原报告。原卡必做条件有效，上级及用户指令优先。

## 1. 默认执行安排

按 B0–B5 串行推进，逐项修改验证，无需逐项批准。阻塞后继续独立项，依赖项不得报通过。优先级：资料安全、正确、兼容、性能、整理。

每项先写触发步骤、正确结果、待验证原因，再做最小修改。完成即更新 PROGRESS.md，换会话读进度续做。可调整方法，不降低验收强度。

## 2. 修改与数据边界

产品修改限原卡白名单及已新增 library_db.py、browse_queries.py。允许针对缺陷完善 validation/repair_* 和最小适配 validate.py 的现役页面步骤；不改视觉方向，不加框架、依赖或模型，不做额外重构。

原卡、原完成报告、独立 REVIEW.md、原探针及其输出只读。新证据写 validation/reports/code-quality-repair-20260910-r2/；每次运行新建 run 目录，旧红灯证据不覆盖。合成数据写 validation/work/ 下本轮独享目录。

不操作正式 data/，不停止或重启 8765，不改扫描、已识别人脸、识别阈值或隐藏的物体入口，不改原照片或 EXIF。导入 app 前设置隔离 PHOTO_LIBRARY_DATA；服务启动后核对 /api/status 的目录和 PID，再允许写入。测试使用当前 web/，不指向历史页面。只停止已核实由本轮创建的进程。

## 3. B0：固定基线与建立可用的检查

1. 核实解释器、Node、端口、数据目录；Python 以启动器当前可用解释器为准。
2. 保存开工源码及测试 SHA256、before/ 快照。旧 manifest 有五处不匹配；当前阈值 0.50，旧快照 0.60。查明修改来源；无授权证据时保留当前值、登记待核实，不擅自恢复或继续调值，不因此停掉其他修复。整体验收须交代该差异。
3. 原卡所有用例及本卡检查点逐条写 results.json，初始 NOT_RUN；聚合 PASS 不能代替子用例。
4. 复制独立 probe_viewer.js、probe_python.py 到新报告目录的同一目录深度；仅改副本工作目录/输出路径以保留原证据。保存接线 diff，保持断言和期望。Python 原 WORK 写死到旧测试场，必须改为本轮目录。不要直接运行原件覆盖旧结果。
5. Node 探针依赖 repair_viewer_checks.js；Python 抽取现役索引函数。接口变化只适配接线，或补等价检查并保留原件；加载失败不等于修复。

原验收复现 Node 两个、Python 三个失败。副本核验接线后运行（以下副本尚未运行）：

    node validation/reports/code-quality-repair-20260910-r2/probe_viewer.js
    & 'C:/Users/A/PycharmProjects/ImageBrowser/.venv/Scripts/python.exe' -X utf8 validation/reports/code-quality-repair-20260910-r2/probe_python.py

先留失败输出和非零退出码；若当前已通过，查清代码变化，不为制造红灯改现役实现。先修正原目录检查的 `or True` 占位断言，证明错误范围会被拒绝，再用它验证后续修复。

## 4. B1–B5：逐项实施与验收

### B1 / R03：大图状态

入口：web/viewer.js 的 openPhoto、movePhoto、ensureViewerWindow。

- 大图仍打开：打开 100→前进 101→由另一入口打开 400→前进，结果必须 401，不能 102。普通关闭重开另测，不能混同场景。
- 把显示 101 的过程延迟，其间再前进一步，释放延迟后必须显示 102，不能遗留无人处理的 queued。
- 旧窗口请求延迟时切到新目标，再释放旧请求；旧结果不得覆盖新会话的 ids、offset、total、显示内容。
- 用至少 500 张、排序与 ID 不一致的数据，真正越过当前窗口两端；断言发生新窗口请求、offset 改变、相邻 ID 与独立完整排序一致。不能把第 199→202 张自动当作跨窗口。
- Home/End 核对精确序号和 ID；真实浏览器验证快速连按与筛选范围。不能只判断序号包含 `/`。

用可控延迟复现时序，执行现役算法，不硬编码目标 ID；补真实浏览器验证。

### B2 / R04：并发索引

入口：app.py 的 load_face_index、invalidate_face_index、process_faces。

查询结束、发布索引前触发失效：不能让调用方收到未处理的 None 或使用已失效的旧索引。选择可终止的重试或明确可恢复路径，不无限重试。补原 R04 的提交后发布、回滚与合并/忽略交错检查，确认数据库、磁盘小图、内存索引一致。只通过小图 IO 失败回滚不足以完成。

### B3 / R07：地点迁移与合法值

入口：library_db.py 的迁移、规则写入及约束。

- 首次从旧 JSON 导入纬度 40，数据库人工改 41；旧 JSON 改缩进或内容后再次启动，仍必须 41。一次成功迁移后，旧文件不能自动覆盖现役规则。
- 纬度范围 [-90,90]、经度 [-180,180]、半径大于 0，数值必须有限；测试非法坐标、负/零半径、NaN/Infinity、缺字段。不得留下非法行或将失败整体标 ok；合法边界值可保存。
- 不把既存非法数据自动删除或夹到边界；隔离验证升级失败可回滚并提供可定位诊断，人工纠正后可恢复。
- 覆盖合法旧库升级、损坏 JSON、失败后修正重试、重复启动不覆盖、两规则并发保存，以及备份恢复后无需旧 JSON 仍能读取。执行 integrity_check 和 foreign_key_check。正式库不迁移。

### B4 / R08：补性能证据

沿用原卡方法：至少 50,000 档案、100,000 文件，多副本、缺失值和多脸；固定 seed。用原第一轮 before/ 源码作修复前基线，不能把第二轮开工版冒充优化前版本。

同一合成库、参数和 PRAGMA，预热一次，各测至少五次；记录全部原值、中位数、实际 SQL trace、查询计划，覆盖原卡五类场景。ID、顺序、total、计数一致；列表 SQL 不读取 metadata，详情仍完整；至少一项热点成本下降。回退超 20% 按原卡调查，不挑样本。

基线有缺陷时标明预期修正，不改回错误；性能测语义等价查询，缺陷另验。缺可比证据记未完成。

### B5 / R01–R10：完整覆盖与现役页面

补齐原卡未跑场景，特别是目录组合、合并目标、人物 50/97 张分页及连点。复用专项、语法、smoke 与适用的隔离核心回归，按平台要求使用浏览器工具或允许的自动化，工具名称不适用时记录等价替代。

R10 先点击“详细资料”→“补录时间、地点与备注”，等待可见后填写；保存后同时核对页面、API 和原始元数据。不得删 class、force fill 或给隐藏字段赋值代替用户操作。该路径已有独立成功证据，修正步骤后再测不是原样盲跑第四次。必须继续完成此前未执行的后续断言。

验证当前 web/ 在 390/768/1280 宽度的照片流、大图、人物弹窗；原图 SHA256 不变、隔离库完整、无未解释脚本错误、物体入口仍隐藏。

## 5. 防止再次出现无效验收

新检查先证明能识别错误：用开工失败版本，或隔离副本中的受控错误验证红灯；还原后再验绿。不得修改正式源码造失败。可模拟 IO、模型和延迟，不能模拟掉被测 SQL、排序、事务或用假页面代替当前页面。

禁止恒真、删断言、吞异常报成功、硬编码结果、降低验收阈值。smoke 合规 SKIP 单列，必测项 SKIP/NOT_RUN/BLOCKED 均不算通过。失败最多三轮针对性修复，逐轮写新证据；仍失败则隔离本项回退、标 BLOCKED、继续独立项。达到重试上限不代表完成。

## 6. 交付与完成条件

1. 原卡所有必做用例、本卡补测及当前页面检查都有最终版本证据；修复前失败可复现，修复后真实路径成立。未满足则整体 PARTIAL。
2. 未越界；最终源码与被测版本哈希一致，阈值等来源差异已解释。后续改源码必须重验受影响项，不沿用旧绿灯。

交付 PROGRESS.md、BLOCKED.md（无则写无）、COMPLETION.md、results.json、manifest.json、逐次日志、截图、基准原值。沿用原完成报告模板，并在每个用例记录：期望、实际、命令/操作、退出码、证据路径、被测哈希和尝试次数。

报告修复、遗留、验证和位置。全部满足写“执行完成，待独立复核”，否则“部分完成”；明确正式后端未加载，不能称已上线。
