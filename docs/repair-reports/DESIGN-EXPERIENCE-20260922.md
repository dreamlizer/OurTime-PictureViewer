# 设计与体验优化收口报告

日期：2026-09-23  
任务卡：`docs/设计与体验优化任务卡-20260922.md`  
实施基线：`a52d569 Complete homepage memories and launcher reliability`

## 结论

本轮真正影响使用体验的四项问题已经完成：回忆播放器可暂停并稳定结束、已命名人物优先看照片、首页低频动作收进“更多”、大图主题和窄屏工具更容易找到。正式 8765 已只读观察这些前端结果，未重启服务、未让写请求到达正式后端。

任务卡与现有实现存在较多重叠，尤其是 T4 大图工具、T5 首页结构和连续浏览。实施时没有重建这些模块，只补了当前页面仍缺失的交互和视觉层级。重叠没有阻塞后续执行，实际风险是重复改写成熟逻辑和让旧测试选择器失效；本轮通过复用现有入口、迁移旧测试来规避。

当前可以判定“核心实施完成”，但不能把任务卡中所有穷举样本写成已验证。1／2 张回忆、回忆预览失败注入、人物命名／合并写入、超过一页的人物返回、1920／2560 专项测量以及同条件改前截图仍未补齐。

## 实际改动

| 任务 | 实际处理 | 主要文件 |
|---|---|---|
| T1 可读性 | 首页卡片低频动作改为清楚的“更多”菜单；主要按钮和进度点扩大点击区域；保留灰绿配色；补 390px 页面级防溢出 | `web/home-discovery.css`、`web/home-discovery.js`、`web/appearance.css`、`web/home-query-ui.css` |
| T2 回忆播放 | 建立 loading／playing／paused／ended 状态；暂停冻结图像动画；继续从当前图重新给满 6 秒；结束页稳定停留；补焦点圈、两层 Escape、首次片头和 reduced-motion | `web/index.html`、`web/home-discovery.js`、`web/home-discovery.css` |
| T3 人物详情 | 已命名人物首屏突出“查看相关照片”，整理操作默认折叠；待确认人物默认展开命名、合并和路人操作；照片区层级与数量更清楚 | `web/index.html`、`web/app.js`、`web/appearance.css` |
| T4 大图工具 | 复用既有三组工具，不重做查看器；补“定位原文件”完整文案、四个有名称的主题小样及选中态；390px 改为底部横向可滚动工具条 | `web/viewer.js`、`web/viewer-overrides.css` |
| T5 一致性 | 保留三张首页回忆卡和原分类；只修操作层级、间距和窄屏溢出，没有重做推荐、卡片比例或浏览路径 | 上述 CSS／JS |
| T6 验证 | 新增隔离浏览器验收和正式只读检查；迁移旧视觉脚本对默认首页的过时假设 | `validation/test_design_experience_browser.py`、`validation/validate_design_experience_real_readonly.py`、`validation/test_visual_polish_v2.py` |

## 验证结果

### 快速检查

- `validation/smoke.py`：24 项通过，0 跳过。
- `node --check`：`app.js`、`home-discovery.js`、`viewer.js` 通过。
- `py_compile`：本轮三个 Python 验证文件通过。
- `git diff --check`：无空白错误；只有仓库既有的 LF→CRLF 提示。

### 隔离浏览器

证据目录：`validation/reports/design-experience-20260922/20260923-003207/`

- 使用独立 `PHOTO_LIBRARY_DATA`，端口 14648；未连接正式 `data/`。
- 55 项行为断言通过，无 `pageerror`、console warning/error、HTTP error 或失败请求。
- B01–B08 的核心路径已按真实时间执行：暂停 12 秒不前进，继续后 3 秒不跳、约 6 秒推进一张，连续切换 5 次无重复计时器，结束 15 秒不关闭。
- “看这张”关闭后回到同一帧并保持暂停；“看全部”进入原组完整 8 张；“再看一次”和“返回首页”正常。
- 已命名／待确认人物层级、四主题点击、手动标签坐标保留、390px 首页／播放器／大图无页面级横向溢出均通过。
- `validation/test_home_discovery_browser.py`、迁移后的 `validation/test_visual_polish_v2.py` 通过。
- 标签图导出相关 `test_annotated_export.py`、`test_annotated_export_fix.py`、`test_export_and_map_ui_contract.py` 通过并实际打开过 PNG 成品。

### 正式 8765 只读观察

证据目录：`validation/reports/design-experience-20260922/formal-readonly-20260923-004050/`

- health 确认为项目正式 `data/`、`owner=true`；后端运行版本仍为 `252516f…`，没有为本轮重启。
- 已目检真实首页、已命名人物详情、大图主题面板和 390px 首页。
- 新“更多”、人物首屏主操作、四主题、“定位原文件”和窄屏无横向溢出均通过。
- 浏览器层拦截了页面自身 3 次 `/api/ui-ping` POST；它们未到达后端。没有其他业务写请求，没有非预期 console error 或 `pageerror`。
- 正式截图包含私人照片和姓名，只保存在本机验证目录，不作为公开交付物。

## 验收覆盖与未完成项

| 验收组 | 结论 | 边界 |
|---|---|---|
| A01–A05 | 部分通过 | 点击尺寸、菜单键盘操作和焦点返回已验；三类照片逐像素对比度、人物卡全状态并排、换组／隐藏／恢复写路径未专项重跑 |
| B01–B08 | 核心通过 | 8 张样本和全部核心计时／焦点路径已验；1 张、2 张及预览失败／延迟注入未验 |
| C01–C04 | 部分通过 | 已命名／待确认层级和只读浏览已验；命名、合并、错误反馈、第二页返回未专项写入回归 |
| D01–D05 | 通过主要受影响项 | 三组工具、四主题、坐标保留、桌面／390px、标签导出已验；未穷举每个主题的全部字体 |
| E01–E05 | 部分通过 | 1440、390、首页分类／组／大图／返回和真实照片已验；1920、2560、200% 缩放及所有页面首屏未全量巡检 |

没有同条件“改前”截图：本轮开始实施前浏览器控制不可用，不能把历史截图冒充本轮基线。现有证据是完成态截图、行为日志和正式只读观察；审美满意度仍由用户评审。

## 重新评估：哪些还值得做

1. **建议保留，但不阻塞当前使用：**补 1／2 张和预览失败的回忆样本。这是播放器边界测试，不需要继续改视觉。
2. **只有准备正式发布或集中整理人物时再做：**人物命名／合并／第二页返回的隔离写入回归，以及 1920／2560／200% 全尺寸巡检。
3. **不建议继续做：**重排首页三卡、重写大图工具、重做推荐体系、再造主题或全站换肤。这些与现有成熟功能重叠高，收益低且回归面大。
4. **不属于本轮：**重启正式后端、打包绿色版、提交／推送。当前静态前端已被正式页面读取，后端代码仍保持现役进程版本。

## 工作树与数据安全

- 本轮产品改动和验证脚本尚未另行提交或推送。
- 正式服务未停止、未重启，正式照片、数据库和 EXIF 未修改。
- 根目录原有 0 字节异常路径残留未删除、未纳入版本控制。
