# GPS 同名误配及合影遮脸修复

## 已交付

- `geo_labels.py`：已知省份无同名候选时返回空匹配，不退到全国；存在行政区边界时约束候选。跨省/跨区的自动街镇细名不嫁接到当前区。原始本地点名可以在正确区级前缀下保留。
- GeoNames 本地点和 OSM 点分别记录来源，避免统一标成 OSM。
- `tools/repair_place_conflicts.py`：只修正 GPS 边界证明为跨省冲突、且来源为自动地名库的记录，跳过人工地点/用户区域来源；支持只读清单、备份、并发字段核对、逐张审计及重复执行。
- 正式库修正 2,852 张，均由错误外省地点改回北京，其中“小营”117 张。照片 338931 正式接口返回“北京市 · 朝阳区 · 小营”。
- 备份：`data/backups/library-20260919-170215-before-place-hierarchy.sqlite3`。扫描确认 completed 后安全重启正式服务，加载修复代码。地点推荐目录失效后按现有入口重新生成。
- `viewer.js`：密集合影补充更大上下位移候选，先避脸，再处理标签碰撞。远距离候选仅在近处避让条件较差时使用。字号和自定义坐标不变。

## 验证

- 地点层级和修复脚本共 11 项测试通过，包含同名跨省、同省跨区、人工信息保留、并发修改拒绝、扫描期间拒绝和幂等。
- 隔离 HTTP 新照片扫描通过：从合成 EXIF GPS 正确生成“北京市 · 朝阳区 · 小营”，来源 GeoNames，原图 SHA256 不变。初次验证的 Windows 临时锁清理失败已改为调用正常 shutdown，重跑退出码 0。
- 正式备份对比：人工地点、GPS、人名标签自定义记录无变化；自动地点恰好变化 2,852 条。再次只读计划为 0 条。
- 标签浏览器回归 33 项通过；真实照片 338294 的正式浏览器预览 7 个标签与人脸框重叠面积为 0，并人工查看截图。默认字体的真实预览是 `validation/reports/place-conflicts-20260919/labels-live.png`。
- 日常 smoke 24 项通过。
- 旧全流程 `validation/validate.py` 未通过：首页查询栏默认隐藏导致入口等待超时，未进入扫描断言。本轮未把它记为通过，也未保留尝试修改的选择器；本次入库路径改用 `validation/validate_place_scan.py` 独立验证。

## 证据与范围

`validation/reports/place-conflicts-20260919/` 内有改前改后清单、备份位置、入库结果、数据保留对比、剩余冲突计划和正式标签预览。未重新识别人脸或改写原图。

只批量处理已证明的跨省自动地点错误；不宣称所有离线街道/社区细名都是精确地址。密集合影仍可能需要用户按审美微调；已有自定义位置不会自动覆盖。
