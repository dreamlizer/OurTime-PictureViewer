请在「拾光 · 本地照片资料库」本机项目中，按照我提供的《OurTime_Viewer_Display_Refactor_TaskCard_2026-09-26.md》执行重构。先完整阅读任务卡、最新 AGENTS.md 和实际代码；01032b2 是核对基线，不是要求回退的版本。保留后续有效修改。

按 P0→P1→P2→P3→P4→P5 分阶段实施，不一次性全面重写。每阶段先做最小闭环，完成该阶段真实运行验收和本地可恢复检查点后再继续。普通失败自行修复，不带着未解释的错误进入下一阶段，也不需要每阶段问我是否继续。

核心是统一“已提交画面”的状态与 commitDisplay 采用入口，把正在加载的下一张与当前画面分开。load、zoom、字体、主题、资源和业务回执只能经有身份校验的通知/刷新入口。关闭立即使旧会话失效，但保留必要静态画面完成渐隐。已发送的用户写操作仍按原实体处理，不因关闭而丢失，也不能回写新照片。

先处理大图状态和隐藏的 renderPhoto 包装接线，再处理大图动效/导出，最后处理页面和同容器照片流换源。一次切换只能有一个内容透明度 owner；保留原有标签主题、手动坐标、字体底板、导出冻结与渐隐渐入合同，不扩展业务、不改识别/合并/后端数据语义。

日常仍跑 python validation/smoke.py。本次关键阶段还必须加载最终 index 和真实脚本、实际打开照片及样式面板并执行退出再进和切换；网络层可以使用隔离 fixture，但不能 stub 实际控制器/布局器后声称全入口通过。最后一次修改后必须重测，记录浏览器实际加载文件与交付文件的对应关系。语法检查、纯函数测试或一张最终截图不能替代运行验收。

所有写入和真实导出验证使用独立 PHOTO_LIBRARY_DATA，不触碰正式照片，不重扫或重识别，不覆盖用户未提交工作。修好后安全应用到我日常使用的本机版本并检查实际生效；遇到扫描或数据安全等真实阻塞时准确说明。不要自行推送 GitHub、上传报告、创建远程分支、PR 或 Issue。只在本地交付代码、必要测试和阶段结果，明确 PASS/FAIL/BLOCKED/NOT_RUN 及验证层级。
