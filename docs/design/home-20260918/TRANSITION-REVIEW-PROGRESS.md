# 过场检查进度 · 2026-09-19

未重启正式 8765，未写推荐后端。静态 JS/CSS 已由 8765 直接提供。

## 源码改动

- 首页：按类别缓存；切 Tab 不再整板换成矮的“加载中”；超过 220ms 才出右上角“更新中”；换一组只替换对应卡片；查看更多改为追加。
- 分页：去掉切页整板透明/上移/半透明三段；照片流保留高度，去掉互相纠正的 scrollTo。
- 返回：恢复滚动期间 main 先 hidden，避免先闪顶部。
- 大图：缩略图承接；加载层改为角落小提示且延迟 220ms；不再全幅黑罩。

## 标签与人名（同轮）

- 硅谷八人：英文名为姓名，中文名为别名。
- 别名按钮三态：姓名 → 姓名/别名 → 仅别名（无别名仍显示姓名）。
- 排版三态：自动 / 横排 / 竖排。自动规则：至少两个拉丁字母且无汉字则为英文，横排；否则竖排。

## 验证

见本轮测试命令结果。正式服务 Python 仍为 7c314d0，前端静态文件已是工作树版本。
