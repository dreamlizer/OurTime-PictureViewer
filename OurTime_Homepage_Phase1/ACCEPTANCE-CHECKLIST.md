# 验收清单

## A. 本包已完成的检查（不是本地项目验收）

结果见 `evidence/browser-checks.json`：40 PASS、0 FAIL；浏览器 Chromium 144.0.7559.96。使用浏览器内存中的合成宿主，不启动 Python 应用，不访问数据库/照片，不发外网或写请求。

检查覆盖：默认隐藏抽屉与条件行；筛选草稿/取消/应用；一次应用一次回调；筛选与排序独立；关键词、人物、目录条件移除；中文 IME 与输入防抖；人物本地搜索与 5,201 项合成选项下的渲染上限；已有未知/组合人物 ID 的保留；目录选择取消/迟到回调；批量入口/选择数量/恢复模式；错误和过期状态；键盘焦点与 Esc；销毁重挂；动态文字安全；320/390/768/1280/1440 宽度；旧式全局 CSS 冲突；扁平 state 桥的原子更新、失败回滚和旧控件值同步。

Node 语法检查和仅新增文件补丁的验证另见 `evidence/static-and-patch-checks.json`。

## B. Luna 必须在本地核对的接线

- [ ] 确认当前仓库/工作区基线，已有修改全部保留；没有拉远程覆盖本地。
- [ ] 三个新文件准确加载；保留 app → viewer → waterfall 的相对顺序。
- [ ] UI 挂载成功；不重复挂载；失败时没有隐藏原入口形成白屏。
- [ ] 首页可见标题为“全部照片”，内部 view 标识未重命名；默认排序仍最新优先。
- [ ] 重复的时间排序、目录输入等不再常驻首页；没有凭空新增时间线、标签或占位条件。
- [ ] 非首页页面原工具栏与功能正常，不能携带用户看不见且无法清除的筛选。
- [ ] 原 person-filter/搜索/sort 等 DOM 引用被正确保留或调整，无 null/undefined 错误，无重复监听。
- [ ] `reloadPhotos` 调的是从头重建当前范围，不是加载下一页；现有 offset/maxId/缓存/generation 合同保留。
- [ ] 不重复调用加载器；人物+目录+关键词是同一次查询，而不是三个依次覆盖的操作。
- [ ] 人物列表读的是已有真实数据源，核对分页完整性；未把已加载一页当作全库。
- [ ] 文件夹选择器只返回草稿路径，不扫描、不提前应用。未接入时如实报告。
- [ ] 导航、人物跳转、旧入口改条件、照片勾选与任务状态更新均调用新 UI 的 sync。
- [ ] 批量动作直接复用既有处理函数和确认链路，而非模拟隐藏按钮点击或新写 API。

## C. 真实只读浏览检查

下面每项记录 PASS / FAIL / SKIP / 未验证，不能用模块测试代替。

- [ ] 首页搜索得到正确结果；清空关键词回到原范围。
- [ ] 人物筛选有效；真实人物姓名标签对应真实 ID。
- [ ] 人物+关键词、人物+目录、人物+目录+关键词的结果与旧查询语义一致。
- [ ] 文件夹筛选包括既有定义下的子目录；不擅自改变大小写、目录分隔符或排序。
- [ ] sort 四种模式符合现有实现，添加/删除条件不重置用户的排序。
- [ ] 从人物详情跳回首页后，新工具栏显示该人物条件，结果范围正确。
- [ ] 年份入口到现有 years 页面，选年后仍能查看原年份范围，没有新增一套时间聚合。
- [ ] 条件、排序、年份等范围下打开真实大图，前后翻页不串范围；关闭返回原滚动位置。
- [ ] 长列表继续加载/向上回看正常，虚拟化数量和说明高度没有被新 UI 改动。
- [ ] 文件夹、地点、人物及原已排除页面仍可用，旧导航无断链。
- [ ] 点击“选择”前无新勾选框；进入后沿用原 checkbox 和选中 Set。
- [ ] “选择当前屏幕”只选择可见照片；跨滚动选中状态和 1000 张上限沿用当前项目规则。
- [ ] 取消选择回到查询栏，未更改条件、排序和原照片。
- [ ] 排除/补录弹窗可打开并取消；**不在正式数据上提交**。
- [ ] 1280、768、390 宽度无按钮遮挡或横向溢出；抽屉能滚动，底部按钮可达。
- [ ] 页面没有控制台错误；抽屉默认不显示；首页没有设计图中的下方示意面板。

## D. 写操作与真实服务边界

排除提交、恢复提交、批量补录提交只在隔离数据/既有隔离测试中核验，不对用户正式库自动执行。模块测试的“动作回调触发”不能写成“真实排除成功”。

不要停止/重启现役 8765、暂停/恢复扫描、重算人脸、修改参数。先遵循项目现有 smoke 规则；需要额外服务时必须明确指定独立端口和 `PHOTO_LIBRARY_DATA`，但本包自测不需要服务。

## E. 最终报告至少包含

1. 实际修改/新增文件和小范围说明。
2. 接入的真实函数、DOM 节点、同步钩子和照片重建路径。
3. 实际运行命令及结果，明确模块/真实页面/隔离写操作的验证范围。
4. 已存在的旧测试问题与本轮新增问题分开记录。
5. 未验证事项、尚未接通的可选功能、阻塞问题。
6. 只回退本轮修改的操作方法；不使用整库 reset/clean。
